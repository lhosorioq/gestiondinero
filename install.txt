Entre en la carpeta "server" y ejecute:
npm install

Luego en la misma carpeta ejecute:
npm run devbabel

En caso de que el servidor no cargue instalar nodemon local
npm install nodemon

Seguido..
Entre en la carpeta "client" y ejecute:
npm install 

Luego en la misma carpeta ejecute:
npm run serve

En caso de error de createPersistedState se debe instalar 
npm install --save vuex-persistedstate